import 'package:flutter/material.dart';

class AllBooksPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('All Books'),
      ),
      body: Center(
        child: Text('All Books Page - Coming Soon'),
      ),
    );
  }
}
